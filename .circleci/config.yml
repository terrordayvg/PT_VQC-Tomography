

version: 2.1
orbs:
  codecov: codecov/codecov@3.2.5
  
jobs:
  build:
    docker:
      - image: circleci/python:3.11.6-node
    steps:
      - checkout
      - run:
          python -m pip install --upgrade pip
      codecov/upload
