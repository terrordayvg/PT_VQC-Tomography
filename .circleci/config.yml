
version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.15
    steps:
      - run:
          name: The First Step
          orbs:
               codecov/codecov@3.2.4
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'

