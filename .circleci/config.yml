

version: 2.1
orbs:
  codecov: codecov/codecov@3.2.5
  
jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: npm install
      - run: npm test
      codecov/upload:
        file: coverage/*.json
