

version: 2.1
orbs:
  codecov: codecov/codecov@3.2.5
  
jobs:
  build:
    docker:
      - image: circleci/python:2.7.15-node
    steps:
      - checkout
      - run:
          python -m pip install --upgrade pip
